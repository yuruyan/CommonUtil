<Page x:Class="CommonUtil.View.IdiomMatchingView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CommonUtil.View" 
      xmlns:ui="http://schemas.modernwpf.com/2019"
      mc:Ignorable="d" 
      DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="IdiomMatchingView">

  <Grid Margin="8,8,0,8">
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition/>
    </Grid.RowDefinitions>

    <ui:SimpleStackPanel Orientation="Horizontal"
                         HorizontalAlignment="Center"
                         Spacing="8"
                         Margin="0,8,0,8">
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="输入成语："/>
        <ui:AutoSuggestBox MinWidth="150"
                           FontSize="{StaticResource SystemFontSize}"
                           FontFamily="{StaticResource SystemFontFamily}"
                           VerticalAlignment="Center"
                           KeyUp="IdiomInputBoxKeyUp"
                           TextChanged="InputIdiomChanged"
                           Text="{Binding InputIdiom,Mode=TwoWay}" />
      </StackPanel>
      <Button Click="GenerateClick">
        <StackPanel Orientation="Horizontal">
          <TextBlock Style="{StaticResource ButtonIconFontStyle}" Text="&#xe601;"/>
          <TextBlock Text="生    成" Style="{StaticResource ButtonTextBlockStyle}"/>
        </StackPanel>
      </Button>
    </ui:SimpleStackPanel>

    <ListBox x:Name="ResultListBox"
             Grid.Row="1"
             Background="Transparent"
             ItemsSource="{Binding MatchList}">
      <ListBox.ItemContainerStyle>
        <Style TargetType="ListBoxItem" BasedOn="{StaticResource DefaultListBoxItemStyle}">
          <Setter Property="Padding" Value="4,0,4,0"/>
        </Style>
      </ListBox.ItemContainerStyle>
      <ListBox.ItemsPanel>
        <ItemsPanelTemplate>
          <StackPanel Margin="0"/>
        </ItemsPanelTemplate>
      </ListBox.ItemsPanel>
      <ListBox.ItemTemplate>
        <DataTemplate>
          <StackPanel Background="Transparent">
            <TextBlock Text="{Binding}"
                       Margin="0,12,0,12"/>
            <StackPanel.ContextMenu>
              <ContextMenu>
                <ContextMenu.Resources>
                  <Style TargetType="StackPanel">
                    <Setter Property="Orientation" Value="Horizontal"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Width" Value="120"/>
                  </Style>
                  <Style TargetType="TextBlock" BasedOn="{StaticResource GlobalTextBlockStyle}">
                    <Setter Property="Padding" Value="0,8,0,8"/>
                  </Style>
                  <Style TargetType="TextBlock" x:Key="IconFontStyle" BasedOn="{StaticResource IconFontStyle}">
                    <Setter Property="Margin" Value="8,0,8,0"/>
                  </Style>
                </ContextMenu.Resources>
                <ContextMenu.ItemContainerStyle>
                  <Style TargetType="MenuItem"/>
                </ContextMenu.ItemContainerStyle>
                <StackPanel MouseUp="CopyCurrentResultMouseUp">
                  <TextBlock Style="{StaticResource IconFontStyle}" Text="&#xe6ef;"/>
                  <TextBlock Text="复制"/>
                </StackPanel>
                <StackPanel MouseUp="CopyAllResultsMouseUp">
                  <TextBlock Style="{StaticResource IconFontStyle}" Text="&#xe6ef;"/>
                  <TextBlock Text="复制全部"/>
                </StackPanel>
              </ContextMenu>
            </StackPanel.ContextMenu>
          </StackPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </Grid>
</Page>
