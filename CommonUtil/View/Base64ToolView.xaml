<Page x:Class="CommonUtil.View.Base64ToolView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CommonUtil.View" 
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:widget="clr-namespace:CommonUtil.Widget"
      mc:Ignorable="d" 
      Margin="8"
      DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Base64Tool">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
      <RowDefinition/>
    </Grid.RowDefinitions>

    <!-- Control -->
    <WrapPanel Orientation="Horizontal">
      <WrapPanel.Resources>
        <Style TargetType="widget:IconButton" BasedOn="{StaticResource GlobalIconButtonStyle}">
          <Setter Property="Margin" Value="0,0,8,8"/>
        </Style>
      </WrapPanel.Resources>
      <widget:IconButton Icon="&#xe643;" Text="字符串编码" Click="EncodeString"/>
      <widget:IconButton Icon="&#xe644;" Text="字符串解码" Click="DecodeString"/>
      <widget:IconButton Icon="&#xe643;" Text="文件编码" Click="EncodeFile"/>
      <widget:IconButton Icon="&#xe644;" Text="文件解码" Click="DecodeFile"/>
      <widget:IconButton Icon="&#xe6ef;" Text="复制结果" Click="CopyResultClick"/>
      <widget:IconButton Icon="&#xe946;" 
                         Text="清空输入"
                         Margin="0,0,8,8"
                         Style="{StaticResource GlobalErrorButtonStyle}"
                         Click="ClearInputClick"/>
    </WrapPanel>
    <!-- Description -->
    <UniformGrid Grid.Row="1" Columns="2" Margin="0,8,0,8">
      <UniformGrid.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource GlobalTextBlockStyle}">
          <Setter Property="FontSize" Value="{DynamicResource HeaderFontSize}"/>
        </Style>
      </UniformGrid.Resources>
      <TextBlock Text="输入："/>
      <TextBlock Text="结果：" Margin="4,0,0,0"/>
    </UniformGrid>
    <!-- Input,Output -->
    <UniformGrid Grid.Row="2" Columns="2">
      <TextBox Text="{Binding InputText}"
               Margin="0,0,4,0"
               Style="{StaticResource MultilineTextBoxStyle}"/>
      <TextBox Text="{Binding OutputText,Mode=OneWay}"
               Style="{StaticResource MultilineTextBoxStyle}"
               Margin="4,0,0,0"
               IsReadOnly="True"/>
    </UniformGrid>
  </Grid>
</Page>
