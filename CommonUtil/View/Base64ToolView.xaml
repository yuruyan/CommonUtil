<Page x:Class="CommonUtil.View.Base64ToolView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CommonUtil.View" 
      xmlns:ui="http://schemas.modernwpf.com/2019"
      mc:Ignorable="d" 
      Margin="8"
      DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Base64Tool">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
      <RowDefinition/>
    </Grid.RowDefinitions>

    <!-- Control -->
    <WrapPanel Orientation="Horizontal">
      <WrapPanel.Resources>
        <Style TargetType="StackPanel">
          <Setter Property="Orientation" Value="Horizontal"/>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource GlobalButtonStyle}">
          <Setter Property="Margin" Value="0,0,8,8"/>
        </Style>
      </WrapPanel.Resources>
      <Button Click="EncodeString">
        <StackPanel>
          <TextBlock Style="{StaticResource ButtonIconFontStyle}" Text="&#xe643;"/>
          <TextBlock Text="字符串编码" Style="{StaticResource ButtonTextBlockStyle}"/>
        </StackPanel>
      </Button>
      <Button Click="DecodeString">
        <StackPanel>
          <TextBlock Style="{StaticResource ButtonIconFontStyle}" Text="&#xe644;"/>
          <TextBlock Text="字符串解码" Style="{StaticResource ButtonTextBlockStyle}"/>
        </StackPanel>
      </Button>
      <Button Click="EncodeFile">
        <StackPanel>
          <TextBlock Style="{StaticResource ButtonIconFontStyle}" Text="&#xe643;"/>
          <TextBlock Text="文件编码" Style="{StaticResource ButtonTextBlockStyle}"/>
        </StackPanel>
      </Button>
      <Button Click="DecodeFile">
        <StackPanel>
          <TextBlock Style="{StaticResource ButtonIconFontStyle}" Text="&#xe644;"/>
          <TextBlock Text="文件解码" Style="{StaticResource ButtonTextBlockStyle}"/>
        </StackPanel>
      </Button>
      <Button Click="CopyResultClick">
        <StackPanel>
          <TextBlock Style="{StaticResource ButtonIconFontStyle}" Text="&#xe6ef;"/>
          <TextBlock Text="复制结果" Style="{StaticResource ButtonTextBlockStyle}"/>
        </StackPanel>
      </Button>
      <Button Click="ClearInputClick"
              Margin="0,0,8,8"
              Style="{StaticResource GlobalErrorButtonStyle}">
        <StackPanel>
          <TextBlock Style="{StaticResource ButtonIconFontStyle}" Text="&#xe946;"/>
          <TextBlock Text="清空输入" Style="{StaticResource ButtonTextBlockStyle}"/>
        </StackPanel>
      </Button>
    </WrapPanel>
    <!-- Description -->
    <UniformGrid Grid.Row="1" Columns="2" Margin="0,8,0,8">
      <UniformGrid.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource GlobalTextBlockStyle}">
          <Setter Property="FontSize" Value="{DynamicResource HeaderFontSize}"/>
        </Style>
      </UniformGrid.Resources>
      <TextBlock Text="输入："/>
      <TextBlock Text="结果：" Margin="4,0,0,0"/>
    </UniformGrid>
    <!-- Input,Output -->
    <UniformGrid Grid.Row="2" Columns="2">
      <TextBox Text="{Binding InputText}"
               Margin="0,0,4,0"
               Style="{StaticResource MultilineTextBoxStyle}"/>
      <TextBox Text="{Binding OutputText,Mode=OneWay}"
               Style="{StaticResource MultilineTextBoxStyle}"
               Margin="4,0,0,0"
               IsReadOnly="True"/>
    </UniformGrid>
  </Grid>
</Page>
